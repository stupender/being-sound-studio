--- a/src/ui/links.ts
+++ b/src/ui/links.ts
@@ -18,6 +18,12 @@
  */
 export const SUBSTACK = 'https://beingsound.substack.com';
 
+/**
+ * One-to-one lessons. This app is a map; the teaching is the other half, so
+ * the footer offers it once, quietly, after the Substack.
+ */
+export const LESSONS = 'https://beingsound.studio/music-lessons';
+
 /** Shown beside the copyright. */
 export const AUTHOR = 'Stu Pender';
 
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -44,6 +44,7 @@
   BEING_SOUND,
   FEEDBACK_EMAIL,
   FEEDBACK_SUBJECT,
+  LESSONS,
   SUBSTACK,
 } from './ui/links';
 import { alreadyAsked, markDismissed } from './ui/asked';
@@ -603,12 +604,30 @@
           {/* One invitation, and only once there's somewhere to send people.
               See ui/links.ts — an unset address renders nothing at all rather
               than a link that goes nowhere. */}
-          {SUBSTACK && (
+          {/* One sentence for both outward links, rather than two lines each
+              opening with the same name. Either clause drops out on its own if
+              its URL is unset — the empty-means-hidden rule in ui/links.ts. */}
+          {(SUBSTACK || LESSONS) && (
             <p>
-              Stu writes about practice and music at{' '}
-              <a href={SUBSTACK} target="_blank" rel="noreferrer">
-                his Substack
-              </a>
+              Stu{' '}
+              {SUBSTACK && (
+                <>
+                  writes about listening and expression at{' '}
+                  <a href={SUBSTACK} target="_blank" rel="noreferrer">
+                    his Substack
+                  </a>
+                </>
+              )}
+              {SUBSTACK && LESSONS && ', and '}
+              {LESSONS && (
+                <>
+                  offers{' '}
+                  <a href={LESSONS} target="_blank" rel="noreferrer">
+                    1:1 music lessons
+                  </a>{' '}
+                  through Being Sound
+                </>
+              )}
               .
             </p>
           )}
--- a/index.html
+++ b/index.html
@@ -45,9 +45,13 @@
       property="og:description"
       content="A living textbook for the fretboard: scales, harmony and ear training, seen as constellations of possibility across the whole neck. So you can just play."
     />
-    <meta property="og:url" content="https://stupender.github.io/method/" />
-    <meta property="og:image" content="https://stupender.github.io/method/icon-512.png" />
-    <meta name="twitter:card" content="summary" />
+    <meta property="og:url" content="https://beingsound.studio/fretboard-constellations/" />
+    <meta property="og:image" content="https://beingsound.studio/images/projects/Method.png" />
+    <meta property="og:image:width" content="1080" />
+    <meta property="og:image:height" content="1080" />
+    <meta property="og:image:alt" content="Fretboard Constellations — scales and harmony as colour-coded shapes across the neck, with notation below." />
+    <meta name="twitter:card" content="summary_large_image" />
+    <meta name="twitter:image" content="https://beingsound.studio/images/projects/Method.png" />
     <meta name="twitter:title" content="Fretboard Constellations" />
     <meta
       name="twitter:description"
